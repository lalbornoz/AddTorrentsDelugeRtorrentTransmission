"
" Disable Vi backwards compatibility at the very start (as per the Vim
" documentation: "This is a special kind of option, because when it's set
" or reset, other options are also changed as a side effect. NOTE: Setting or
" resetting this option can have a lot of unexpected effects: Mappings are
" interpreted in another way, undo behaves differently, etc.  If you set this
" option in your vimrc file, you should probably put it at the very start.")
"
set nocompatible

"
" Source set of run-command Vim files setting/adding Vim options, plugin options,
" commands, autocommands, mappings, and user-local additions, if any.
"
for f in
	\ glob("~/.vim/options.vim.d/*.vim", 0, 1) +
	\ glob("~/.vim/options.pack.d/*.vim", 0, 1) +
	\ glob("~/.vim/commands.d/*.vim", 0, 1) +
	\ glob("~/.vim/commands.auto.d/*.vim", 0, 1) +
	\ glob("~/.vim/mappings.d/*.vim", 0, 1) +
	\ [expand("~/.vim/rc.local")]
	\
	if filereadable(f)
		execute("source " . f)
	endif
endfor | unlet f

"
" Install commands and initialise lightline.
"
call roarie_commands#Install()
call lightline#init()

" vim:filetype=vim noexpandtab sw=8 ts=8 tw=0
